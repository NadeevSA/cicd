#!groovy

pipeline {
    agent any

    stages {
		stage('docker-compose') {
			steps {
				echo "=== docker-compose ==="
				sh """
				docker-compose -f ./ci/docker-compose.yml up
				"""
			}
		}
	}
}