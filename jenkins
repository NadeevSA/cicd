pipeline {
    agent { none }

    stages {
	    stage('build') {
            steps {
                sh 'mvn --version'
            }
        }
		
		stage('docker-compose') {
			steps {
				echo "=== docker-compose ==="
				sh """
				docker-compose -f ./docker-compose.yml up
				"""
			}
		}
	}
}