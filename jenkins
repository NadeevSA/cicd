#!groovy

pipeline {
    agent { dockerfile true }

    stages {
		stage('docker-compose') {
			steps {
				echo "=== docker-compose ==="
				sh """
				docker-compose -f ./docker-compose.yml up
				"""
			}
		}
	}
}