#!groovy

pipeline {
    agent { 
		label 'master'
	}

    stages {
		stage('docker-compose') {
			steps {
				echo "=== docker-compose ==="
				sh """
				docker-compose -f ./docker-compose.yml up
				"""
			}
		}
	}
}