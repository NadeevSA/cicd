- hosts: servers
  tasks:
  - name: copy dockerfile
    ansible.builtin.copy:
      src: ./ci/docker/paint.dockerfile
      dest: /home/serezhka/ansible/paint.dockerfile
      owner: serezhka
      group: serezhka
      mode: 0755

  - name: pull image
    shell:
      cmd: "sudo docker pull 192.168.0.108:5000/app"

  - name: change tag image
    shell:
      cmd: "sudo docker tag 192.168.0.108:5000/app app"

  - name: run container
    shell:
      chdir: /home/serezhka/ansible/
      cmd: "sudo docker run -d -p 8081:8080 --name app paint"
