- hosts: servers
  tasks:
  - name: copy docker-compose
    ansible.builtin.copy:
      src: ../docker/docker-compose.yaml
      dest: /home/serezhka/ansible/docker-compose.yaml
      owner: serezhka
      group: serezhka
      mode: 0755

  - name: run docker-compose
    shell:
      chdir: /home/serezhka/ansible
      cmd: "sudo docker-compose up -d"
