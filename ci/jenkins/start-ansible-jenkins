#!groovy

pipeline {
    agent any

    stages {
        stage('run ansible playbook'){
			steps{
			 	script{
					sh """
					ansible-playbook ./wiki_and_db.yaml -i ./ci/hosts --user=serezhka
					"""
				}
			}
		}
	}
}