#!groovy

pipeline {
    agent any

    stages {
        stage('run ansible playbook'){
			steps{
			 	script{
					sh """
					ansible-playbook ./app.yaml -i ./ci/hosts
					ansible-playbook ./wiki_and_db.yaml -i ./ci/hosts
					"""
				}
			}
		}
	}
}