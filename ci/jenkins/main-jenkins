#!groovy

pipeline {
    agent any

    stages {
		stage('Start app') {
			steps {
                build job: 'Start/app-start-job'
            }
		}

		stage('Start wiki and db') {
			steps {
                build job: 'Start/wiki_and_db-start-job'
            }
		}
	}
}