#!groovy

pipeline {
    agent any

    stages {
        stage('Remove') {
			steps {
				echo "=== remove ==="
				sh """
				sudo docker rmi paint |
				sudo docker rm app |
				sudo docker stop app 
				"""
			}
		}
	}
}